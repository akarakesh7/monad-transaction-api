{
  "name": "monad-transactions-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker for proxying Monad blockchain transaction requests",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler publish",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 